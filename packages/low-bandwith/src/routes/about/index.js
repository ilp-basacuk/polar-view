import Menu from '../../components/menu';

const About = () => (
  <div className="bg-navyblue min-h-screen w-full">
		<Menu />
    <h1 className="text-sky">About</h1>
  </div>
);

export default About;
