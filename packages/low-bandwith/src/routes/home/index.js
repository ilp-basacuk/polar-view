import Menu from '../../components/menu';

const Home = () => (
  <div className="bg-navyblue min-h-screen w-full">
		<Menu />
    <h1 className="text-sky">Home</h1>
    <p>This is the Home component.</p>
  </div>
);

export default Home;
